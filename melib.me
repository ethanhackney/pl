struct array {
        arr = []

        # make constructors optional
        func ctor() {
        }

        func push(v) {
                arr.push(v)
        }

        func sort() {
                arr.sort()
        }

        func at(idx) {
                return arr[idx]
        }
}
